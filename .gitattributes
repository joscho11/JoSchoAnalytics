# Auto detect text files and perform LF normalization
* text=auto
# The DFS site verifies projection_csv_sha256 against the served bytes, so
# git must store these CSVs exactly as hashed (no line-ending rewrite).
fantasy/optimizer_projections/*.csv -text
