# U6 weekly fantasy + dfs — Lens B

- U6B-1 - fantasy/ weekly + dfs - zero pytest coverage (notebook inline cells only;
  retrain_models.py untested) - MED - TEST-GAP - screened T8 (projection CSV path
  intact) - risk assessment: low change-frequency; the papermill cron path is gated
  by the notebooks' inline test cells - action: a smoke test for retrain_models
  paths/args would be new CI scope (GATED).

Coverage: as Lens A. NO FINDINGS of severity >= HIGH.
