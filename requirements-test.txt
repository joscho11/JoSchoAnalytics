# Dependencies for the Python test suites.
-r requirements-ci.txt
streamlit==1.59.1
plotly==6.7.0
pytest==9.0.2
# Responsive/viewport suite. The dedicated CI job installs Chromium and
# fails if those cases skip.
playwright==1.62.0
./vendor/jsa_dfs_optimizer-0.2.3-py3-none-any.whl
