# Streamlit Community Cloud runtime only.
#
# Training, backtests, and model-artifact tooling use requirements-ci.txt or
# requirements-test.txt. Help-page explanations are served from a checked JSON
# snapshot, so the live app does not need the ML training/deserialization stack.
# nflreadpy + polars + pyarrow remain for weekly-fantasy actual-stat refreshes.

# Core data
pandas>=2.0,<3.0
numpy>=1.24,<2.0
polars>=1.0,<2.0
pyarrow>=14,<25

# NFL data
nflreadpy>=0.1.5,<0.2

# Dashboard
streamlit==1.59.1
plotly>=5.17,<7.0
requests>=2.28,<3.0
