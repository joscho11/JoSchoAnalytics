# Streamlit Cloud deploy requirements (app.py + the CI deploy-parity job).
# Dashboard's true import closure: streamlit, pandas, plotly, requests,
# nflreadpy (-> polars, pyarrow), numpy. The extra ML/test packages below are
# kept because the CI test suites import them (catboost/scipy/joblib via
# build_draft_board) or repo code needs them (python-dotenv via
# betting/odds_client.py) — flagged where they are not dashboard imports.
# Research/notebook-only packages live in requirements-research.txt.

# Core data
pandas>=2.0,<3.0
numpy>=1.24,<2.0
scipy>=1.10,<2.0          # not a dashboard import; needed by CI tests (build_draft_board)
joblib>=1.3,<2.0          # not a dashboard import; needed by CI tests (build_draft_board)
polars>=1.0,<2.0          # nflreadpy runtime dep
pyarrow>=14,<25           # nflreadpy runtime dep

# Models — FLAG: none of these are dashboard imports. catboost is needed by
# the CI tests (build_draft_board); xgboost/scikit-learn/lightgbm are kept
# conservatively (weekly cron + notebooks pin their own via requirements-ci.txt)
# and are candidates for a future move to requirements-research.txt.
xgboost>=2.0,<4.0
scikit-learn>=1.4,<2.0
lightgbm>=4.0,<5.0
catboost>=1.2,<2.0

# DFS — FLAG: not a dashboard import (the DFS tab renders precomputed CSVs);
# pulp is used by the offline DFS pipeline notebooks (fantasy/dfs/). Kept per
# ruling: do not strip anything the DFS path needs.
pulp>=2.7,<4.0

# NFL data
nflreadpy>=0.1.5,<0.2

# Dashboard
streamlit==1.59.1
plotly>=5.17,<7.0
requests>=2.28,<3.0

# Utilities
python-dotenv>=1.0,<2.0   # not a dashboard import; used by betting/odds_client.py
